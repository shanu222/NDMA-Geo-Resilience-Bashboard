services:
  db:
    image: postgis/postgis:15-3.4
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ndma
    ports:
      - "5432:5432"
    volumes:
      - ndma_pg:/var/lib/postgresql/data

volumes:
  ndma_pg:
