# Copy to .env for local API + Vite
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ndma
JWT_SECRET=change-me-in-production
OPENWEATHER_API_KEY=
PORT=3001
# Optional: absolute API base for production builds (otherwise same-origin)
# VITE_API_BASE=
